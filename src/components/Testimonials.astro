<!-- ---
const url = "";
const api = "";
const options = {
  method: "GET",
  headers: {
    apikey: api,
  },
};

const data = await fetch(url, options).then((res) => res.json());
console.log(data);

import Testimonial from "../components/Testimonial.astro";
---

{
  data.map((testimonial) => {
    return <Testimonial data={testimonial} />;
  })
} -->
<section>
  <!-- <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Quisquam natus porro in molestiae maxime libero commodi quo animi iure optio magni quam fugiat harum facere enim, nobis provident ex excepturi.</p> -->
  <div class="arrow">
    <h2>Det siger vores gæster</h2>
    <img src="src/img/logos/pil-2@2x.png" alt="Royal Escapes logo" />
    <!-- <div class="container"></div> -->
  </div>
</section>

<style>
  section {
    display: flex;
    justify-content: center;
    padding: 2rem;
  }
  .container {
    position: relative;
    height: 300px;
    width: 750px;
    background-color: #252525;
  }
  .arrow img {
    position: relative;
  }
  img {
    width: 1000px;
    height: auto;
  }

  .section h2 {
    position: relative;
    /* top: -140px;
    left: -80px; */
  }

  .container p {
    padding: 2rem;
  }
</style>
